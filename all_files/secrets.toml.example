# ─────────────────────────────────────────────────────────────────
# Streamlit Secrets — for LOCAL development only.
# Copy this to .streamlit/secrets.toml and fill in your values.
# This file is .gitignored — never commit real keys.
#
# On Streamlit Cloud, set these same keys in:
#   App Settings → Secrets
# ─────────────────────────────────────────────────────────────────

SUPABASE_URL = "https://YOUR_PROJECT_ID.supabase.co"
SUPABASE_KEY = "eyJ..."            # Use service_role key (not anon)
DEEPGRAM_API_KEY = "your-deepgram-key"
ANTHROPIC_API_KEY = "your-anthropic-key"
